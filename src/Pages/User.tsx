import React from 'react'
import { useParams } from 'react-router-dom'
import http from '../http';

export const User = () => {
    const {id} = useParams();

    

    return (
        <div>
            
        </div>
    )
}
